[phases.setup]
nixPkgs = ['ffmpeg']

[phases.build]
cmds = [
  'pip install -r requirements.txt',
  'cd backend && python manage.py collectstatic --noinput'
]

[start]
cmd = 'cd backend && python manage.py migrate && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT --workers 3'
